/**
 * The model responsible for sanitizing the input
 * and raising the appropriate errors.
 */

import Hierarchy from "./models/Hierarchy";

export default function Sanitize(target: Hierarchy): void {

    try {

        
    }
    catch (e) {

    }
}
